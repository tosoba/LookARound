package com.lookaround.repo.overpass

import com.lookaround.repo.overpass.dao.SearchAroundDao

internal object OverpassSearchAroundStore {
    fun get(dao: SearchAroundDao, endpoints: OverpassEndpoints) {

    }
}
